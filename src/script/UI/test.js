/* let elemet = document.querySelector(".best-pizza__left-pizza")
options = {
    root: null,
    rootMargin: "50px",
    threshold: 0.1
}



let callback = (entries, observer) => {
    entries.forEach((entry) => {
        entry.target.src = "../assets/images/home/best_pizza_2.png";
        console.log("Yes")
    });
  };


  let observer = new IntersectionObserver(callback, options)
  observer.observe(elemet) */


  console.log("CODE")